CREATE TABLE IF NOT EXISTS "notes".casbin_rule (
    id varchar PRIMARY KEY,
    p_type varchar(255),
    v0 varchar(255),
    v1 varchar(255),
    v2 varchar(255),
    v3 varchar(255),
    v4 varchar(255),
    v5 varchar(255)
);

